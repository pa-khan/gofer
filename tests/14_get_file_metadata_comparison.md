# Тест 14: get_file_metadata (Gofer MCP) vs stat/wc (Native)

## Цель
Сравнить инструмент Gofer `get_file_metadata` с нативными командами `stat` и `wc` для получения метаданных файлов.

## Итерации тестирования

### Итерация 1: Обычный текстовый файл среднего размера
**Задача**: Получить метаданные файла `src/cache.rs`

**Gofer (get_file_metadata)**:
```json
{
  "path": "src/cache.rs"
}
```
**Результат**:
- ✅ Работоспособность: SUCCESS
- Возвращает структурированный JSON:
  - `size_bytes: 16629`, `size_human: "16.2 KB"`
  - `line_count: 515`
  - `is_binary: false`, `is_directory: false`
  - `modified_at`, `created_at` (SystemTime структуры)
- **Количество токенов**: ~180 токенов (структурированный JSON)
- **Скорость**: ~35ms
- **Операций**: 1

**Native (stat + wc)**:
```bash
stat src/cache.rs && wc -l src/cache.rs
```
**Результат**:
- ✅ Работоспособность: SUCCESS
- Возвращает неструктурированный текст:
  - Размер: 16629 байт
  - 515 строк
  - Даты: Доступ, Модифицирован, Изменён, Создан (в локализованном формате)
  - Права доступа: 0644
  - Owner/Group: e5ash/e5ash
- **Количество токенов**: ~350 токенов (многословный текст на русском)
- **Скорость**: ~15ms
- **Операций**: 2 (stat + wc)

**Анализ**:
- **Точность**: Обе 100% - полные метаданные
- Gofer возвращает **структурированный JSON** (легко парсить для LLM)
- Native 2.3x быстрее, но требует 2 операции
- Gofer предоставляет `size_human` (человекочитаемый размер)
- Native включает права доступа и владельца (Linux-специфичные данные)
- **Критично**: Native вывод **локализован** (русский язык) → сложнее для LLM parsing

---

### Итерация 2: Большой файл
**Задача**: Получить метаданные для `Cargo.lock` (большой файл)

**Gofer (get_file_metadata)**:
```json
{
  "path": "Cargo.lock"
}
```
**Результат**:
- ✅ Работоспособность: SUCCESS
- Возвращает:
  - `size_bytes: 242380`, `size_human: "236.7 KB"`
  - `line_count: 10249`
  - `is_binary: false`
- **Количество токенов**: ~185 токенов
- **Скорость**: ~68ms
- **Операций**: 1

**Native (stat + wc)**:
```bash
stat Cargo.lock && wc -l Cargo.lock
```
**Результат**:
- ✅ Работоспособность: SUCCESS
- Размер: 242380 байт
- 10249 строк
- **Количество токенов**: ~355 токенов
- **Скорость**: ~22ms
- **Операций**: 2

**Анализ**:
- **Точность**: Обе 100%
- Gofer медленнее на больших файлах (68ms vs 22ms) - вероятно, из-за подсчёта строк
- Gofer всё ещё более токен-эффективен (185 vs 355 токенов = 48% экономия)
- Native 3.1x быстрее

---

### Итерация 3: Директория
**Задача**: Получить метаданные для директории `src/`

**Gofer (get_file_metadata)**:
```json
{
  "path": "src"
}
```
**Результат**:
- ✅ Работоспособность: SUCCESS
- Возвращает:
  - `is_directory: true`
  - `line_count: null` (для директорий не применимо)
  - `size_bytes: 4096`, `size_human: "4.0 KB"`
  - `is_binary: false`
- **Количество токенов**: ~175 токенов
- **Скорость**: ~28ms
- **Операций**: 1

**Native (stat + file)**:
```bash
stat src/ && echo "---" && file src/
```
**Результат**:
- ✅ Работоспособность: SUCCESS
- Тип: каталог
- Размер: 4096 байт
- Права: 0755/drwxr-xr-x
- **Количество токенов**: ~360 токенов
- **Скорость**: ~14ms
- **Операций**: 3 (stat + echo + file)

**Анализ**:
- **Точность**: Обе 100%
- Gofer корректно определяет `is_directory: true`
- Gofer устанавливает `line_count: null` для директорий (логично)
- Native 2x быстрее, но 3 операции
- Gofer 51% более токен-эффективен

---

### Итерация 4: Несуществующий файл (обработка ошибок)
**Задача**: Запросить метаданные для несуществующего файла

**Gofer (get_file_metadata)**:
```json
{
  "path": "nonexistent_file.txt"
}
```
**Результат**:
- ❌ Работоспособность: ERROR
- Возвращает: `Error: Invalid params: File not found: nonexistent_file.txt`
- Структурированная ошибка (JSON)
- **Количество токенов**: ~40 токенов
- **Скорость**: ~5ms
- **Операций**: 1

**Native (stat)**:
```bash
stat nonexistent_file.txt 2>&1
```
**Результат**:
- ❌ Работоспособность: ERROR
- Возвращает: `stat: не удалось выполнить statx для 'nonexistent_file.txt': Нет такого файла или каталога`
- Exit code: 1
- **Локализованное сообщение** (русский язык)
- **Количество токенов**: ~70 токенов
- **Скорость**: ~8ms
- **Операций**: 1

**Анализ**:
- **Точность**: Обе 100% - корректно обнаруживают отсутствие файла
- Gofer возвращает структурированную ошибку (легко парсить)
- Native возвращает **локализованную ошибку** (зависит от языка системы)
- Gofer 43% более токен-эффективен

---

### Итерация 5: Бинарный файл
**Задача**: Определить, является ли файл бинарным

**Gofer (get_file_metadata)**:
```json
{
  "path": "/tmp/test_binary.bin"
}
```
**Результат**:
- ✅ Работоспособность: SUCCESS
- Возвращает:
  - `is_binary: true` ← **Автоматическое определение!**
  - `line_count: null` (для бинарных файлов не применимо)
  - `size_bytes: 1024`, `size_human: "1.0 KB"`
- **Количество токенов**: ~170 токенов
- **Скорость**: ~32ms
- **Операций**: 1

**Native (stat + file)**:
```bash
stat /tmp/test_binary.bin && file /tmp/test_binary.bin
```
**Результат**:
- ✅ Работоспособность: SUCCESS
- Размер: 1024 байт
- file выводит: `data` (обозначает бинарный файл)
- **Количество токенов**: ~340 токенов
- **Скорость**: ~16ms
- **Операций**: 2

**Анализ**:
- **Точность**: Обе 100%
- **KILLER FEATURE**: Gofer автоматически определяет `is_binary: true`
- Gofer устанавливает `line_count: null` для бинарных файлов (логично)
- Native требует 2 команды (stat + file) для полной информации
- Gofer 50% более токен-эффективен

---

## Сводная таблица

| Метрика | Gofer (get_file_metadata) | Native (stat/wc) | Победитель |
|---------|---------------------------|------------------|------------|
| **Точность (средняя)** | 100% | 100% | 🤝 Ничья |
| **Токен-эффективность** | 150 токенов (среднее) | 295 токенов (среднее) | 🏆 Gofer (49% экономия) |
| **Скорость (средняя)** | 34ms | 15ms | 🏆 Native (2.3x быстрее) |
| **Операций** | 1.0 | 2.2 | 🏆 Gofer |
| **Структурированный вывод** | ✅ JSON | ❌ Текст | 🏆 Gofer |
| **Человекочитаемый размер** | ✅ size_human | ❌ Нет | 🏆 Gofer |
| **Определение бинарных** | ✅ is_binary | ⚠️ Требует file | 🏆 Gofer |
| **Подсчёт строк** | ✅ Встроен | ⚠️ Требует wc | 🏆 Gofer |
| **Права/владелец** | ❌ Нет | ✅ Есть | 🏆 Native |

### Разбивка по точности
1. **Итерация 1** (обычный файл): Gofer 100%, Native 100%
2. **Итерация 2** (большой файл): Gofer 100%, Native 100%
3. **Итерация 3** (директория): Gofer 100%, Native 100%
4. **Итерация 4** (ошибка): Gofer 100%, Native 100%
5. **Итерация 5** (бинарный): Gofer 100%, Native 100%

**Среднее**: Gofer 100%, Native 100%

---

## Ключевые находки

### Преимущества Gofer:
1. **Структурированный JSON**: Легко парсить для LLM агентов
2. **Токен-эффективность**: 49% экономия токенов (150 vs 295)
3. **Человекочитаемый размер**: Автоматическое `size_human: "16.2 KB"`
4. **Определение бинарных файлов**: Встроенное `is_binary: true/false`
5. **Подсчёт строк**: Интегрированный `line_count` (не нужен wc)
6. **Одна операция**: Всё в одном вызове
7. **Язык-агностичные ошибки**: JSON ошибки, не локализованный текст

### Преимущества Native:
1. **Скорость**: 2.3x быстрее в среднем
2. **Права доступа**: Показывает chmod permissions (0644, 0755)
3. **Владелец/группа**: UID/GID информация
4. **Unix метаданные**: Inode, количество блоков, устройство

### Критические отличия:

1. **Локализация**:
   - Gofer: **Язык-агностичный** JSON (всегда на английском)
   - Native: **Локализованный** текст (зависит от системного языка)
   - **Проблема Native**: На русском выводе LLM сложнее парсить "Модифицирован" vs "Modified"

2. **Бинарное определение**:
   - Gofer: **Автоматическое** `is_binary: true`
   - Native: **Требует `file` команду**

3. **Подсчёт строк**:
   - Gofer: **Встроенный** `line_count`
   - Native: **Требует `wc -l`** (дополнительная операция)

4. **Формат вывода**:
   - Gofer: **Структурированный JSON** (парсинг тривиален)
   - Native: **Неструктурированный текст** (требует regex/парсинга)

---

## Рекомендации

### Когда использовать Gofer `get_file_metadata`:
- ✅ LLM агентам нужны метаданные файла
- ✅ Программная обработка (JSON парсинг)
- ✅ Нужна токен-эффективность
- ✅ Проверка бинарных файлов перед чтением
- ✅ Кроссплатформенная консистентность (нет локализации)
- ✅ Нужен человекочитаемый размер ("16.2 KB")

### Когда использовать Native `stat/wc`:
- ✅ Человек проверяет файл в интерактивном режиме
- ✅ Нужна максимальная скорость
- ✅ Нужны права доступа (chmod, владелец)
- ✅ Unix-специфичные метаданные (inode, блоки)
- ✅ Скрипты, где скорость критична

### Гибридный подход:
Для **LLM workflows, требующих метаданных**:
1. Используйте Gofer `get_file_metadata` для принятия решений (бинарный? большой?)
2. Gofer возвращает `is_binary: true` → пропустить чтение
3. Gofer возвращает `size_bytes > 1MB` → использовать chunked read
4. Используйте Native `stat` только для человеческой инспекции

---

## Проблема локализации Native

**Критическая находка**: Native `stat` выводит **локализованный текст**:

**Русская система**:
```
Модифицирован: 2026-02-25 09:44:47
Изменён:       2026-02-25 09:44:47
Создан:        2026-02-19 01:29:18
```

**Английская система**:
```
Modify: 2026-02-25 09:44:47
Change: 2026-02-25 09:44:47
 Birth: 2026-02-19 01:29:18
```

**Проблема для LLM**: Парсинг локализованного текста требует знания языка системы. Gofer решает это, возвращая структурированный JSON с английскими ключами.

---

## Заключение

**Gofer `get_file_metadata` оптимизирован для LLM агентов**, обеспечивая:
- **100% точность** со структурированным JSON выводом
- **49% токен-эффективность** (150 vs 295 токенов)
- **Автоматическое определение бинарных файлов** (`is_binary`)
- **Встроенный подсчёт строк** (не нужен wc)
- **Язык-агностичность** (нет проблем локализации)

**Native `stat/wc` оптимизирован для людей**, предлагая:
- **2.3x более быструю** работу
- **Unix метаданные** (права, владелец, inode)
- **Интерактивный осмотр** с цветным выводом

**Вердикт**: Gofer побеждает для **LLM workflows**, где структурированные метаданные и токен-эффективность критичны. Native побеждает для **интерактивного использования человеком** и скрипты, где нужна максимальная скорость.

Killer feature Gofer: **автоматическое определение `is_binary`** + **язык-агностичный JSON** решают проблемы, с которыми сталкиваются LLM при парсинге локализованного вывода stat.
