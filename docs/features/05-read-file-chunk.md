# 05. Read File Chunk

## Категория
Умное чтение (Resources & Tools)

## Приоритет
🔥 **P0** (Критично)

## Оценка полезности для AI
⭐⭐⭐⭐⭐ (5/5)

## Описание
Построчное чтение файла с указанием диапазона строк.

## Проблема
Читать 10,000-строчный файл целиком — пустая трата токенов. AI часто нужен только конкретный фрагмент кода. Идеально для огромных логов или длинных скриптов.

## Сигнатура
```
read_file_chunk(path, start_line, end_line) -> FileChunk
```

## Параметры
- `path` (string) — путь к файлу
- `start_line` (number) — начальная строка (1-indexed)
- `end_line` (number) — конечная строка (inclusive)

## Возвращаемое значение
```json
{
  "path": "src/large_file.rs",
  "start_line": 100,
  "end_line": 150,
  "content": "    fn process_data() {\n        ...\n    }",
  "total_lines": 5000,
  "has_more_before": true,
  "has_more_after": true
}
```

## Примеры использования

### Пример 1: Чтение конкретной функции
```
AI: get_symbols("src/api.rs")  # узнаёт, что функция handle_request на строке 450
AI: read_file_chunk("src/api.rs", start_line=450, end_line=500)
Result: читает только эту функцию
```

### Пример 2: Анализ логов
```
AI: get_file_metadata("logs/app.log")  # узнаёт, что файл 50 MB, 500k строк
AI: read_file_chunk("logs/app.log", start_line=499900, end_line=500000)
Result: читает последние 100 строк лога
```

### Пример 3: Итеративное чтение
```
AI: read_file_chunk("src/huge.rs", start_line=1, end_line=100)
AI: (анализирует первые 100 строк)
AI: read_file_chunk("src/huge.rs", start_line=101, end_line=200)
AI: (продолжает анализ следующих 100 строк)
```

## Преимущества
- **Экономия токенов**: AI читает только нужный фрагмент
- **Работа с большими файлами**: можно анализировать файлы >1 GB
- **Точность**: AI запрашивает именно ту часть, которая нужна

## Сложность реализации
Низкая (1 день)

## Статус в gofer
❌ Отсутствует, но есть `skeleton` как компромисс

## Зависимости
- filesystem API
- line indexing (можно кешировать позиции строк для быстрого доступа)

## Альтернативы
- `read_file` + обрезка на стороне AI (неэффективно)
- `skeleton` — получить только структуру (но не содержимое)

## Связанные функции
- `get_symbols` — узнать, на какой строке находится функция
- `get_file_metadata` — проверить размер файла
- `skeleton` — получить структуру файла
