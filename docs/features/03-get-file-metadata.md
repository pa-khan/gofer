# 03. Get File Metadata

## Категория
Навигация и анализ (Поиск контекста)

## Приоритет
🟡 **P2** (Полезно)

## Оценка полезности для AI
⭐⭐⭐⭐ (4/5)

## Описание
Возвращает метаданные файла: размер, даты изменения, права доступа.

## Проблема
AI должен принимать решение о том, как читать файл. Если файл 50 MB, читать его целиком — пустая трата токенов. AI может запросить чтение по частям или skeleton.

## Сигнатура
```
get_file_metadata(path) -> FileMetadata
```

## Параметры
- `path` (string) — путь к файлу

## Возвращаемое значение
```json
{
  "path": "src/main.rs",
  "size_bytes": 15420,
  "size_human": "15.4 KB",
  "created_at": "2026-01-15T10:30:00Z",
  "modified_at": "2026-02-20T14:22:00Z",
  "permissions": "rw-r--r--",
  "line_count": 450,
  "is_binary": false,
  "git_status": "modified"
}
```

## Примеры использования

### Пример 1: Проверка размера перед чтением
```
AI: get_file_metadata("logs/app.log")
Result: {"size_bytes": 52428800, "size_human": "50 MB"}
AI: (решает читать файл по частям, а не целиком)
```

### Пример 2: Проверка даты изменения
```
AI: get_file_metadata("src/api.rs")
Result: {"modified_at": "2 hours ago"}
AI: (понимает, что файл недавно изменялся — возможно, есть незакоммиченные изменения)
```

## Преимущества
- **Экономия токенов**: AI не читает огромные файлы целиком
- **Умные решения**: AI может выбрать `read_file_chunk` или `skeleton` для больших файлов
- **Контекст**: AI видит, когда файл последний раз менялся

## Сложность реализации
Низкая (1 день)

## Статус в gofer
❌ Отсутствует

## Зависимости
- filesystem API
- git (для git_status)

## Альтернативы
- AI всегда читает файлы целиком (неэффективно)
- Использовать внешний filesystem MCP

## Связанные функции
- `read_file_chunk` — чтение больших файлов по частям
- `skeleton` — получить только сигнатуры для больших файлов
