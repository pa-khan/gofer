# 11. Move File or Directory

## Категория
Управление структурой (Management Tools)

## Приоритет
🔥 **P0** (Критично для рефакторинга)

## Оценка полезности для AI
⭐⭐⭐⭐⭐ (5/5)

## Описание
Перемещение или переименование файлов и директорий.

## Проблема
Рефакторинг часто требует переноса компонентов в другие модули или переименования файлов. AI должен уметь реорганизовывать структуру проекта.

## Сигнатура
```
move_file_or_directory(source, destination, overwrite) -> MoveResult
```

## Параметры
- `source` (string) — исходный путь
- `destination` (string) — целевой путь
- `overwrite` (boolean, optional) — перезаписать, если целевой файл существует (по умолчанию: false)

## Возвращаемое значение
```json
{
  "source": "src/old_module.rs",
  "destination": "src/modules/auth/handler.rs",
  "status": "moved",
  "type": "file",
  "backup_uuid": "abc123"  // если был overwrite
}
```

## Примеры использования

### Пример 1: Переименование файла
```
AI: move_file_or_directory("src/old_api.rs", "src/new_api.rs")
Result: файл переименован
```

### Пример 2: Перемещение в другую директорию
```
AI: move_file_or_directory("src/auth.rs", "src/modules/auth/handler.rs")
Result: файл перемещён в modules/auth/
```

### Пример 3: Перемещение целой директории
```
AI: move_file_or_directory("src/old_components/", "src/components/legacy/")
Result: вся директория перемещена
```

### Пример 4: Конфликт при перемещении
```
AI: move_file_or_directory("src/api.rs", "src/handlers/api.rs")
Error: {
  status: "conflict",
  message: "File src/handlers/api.rs already exists",
  suggestions: ["Use overwrite=true", "Choose different destination"]
}
```

## Продвинутые фичи

### Автоматическое обновление импортов
При перемещении файла AI должен обновить импорты:

```
AI: move_file_or_directory("src/auth.rs", "src/modules/auth.rs", update_imports=true)

Автоматически обновляет:
- use crate::auth::login; → use crate::modules::auth::login;
- во всех файлах проекта
```

**Статус:** требует интеграции с LSP или парсером импортов

## Преимущества
- **Рефакторинг**: критична для реорганизации кодовой базы
- **Чистота**: AI может навести порядок в структуре проекта
- **Безопасность**: с backup в trash при overwrite

## Сложность реализации
Средняя (2-3 дня)
- Базовое перемещение: низкая
- С update_imports: высокая (требует AST-анализа)

## Статус в gofer
❌ Отсутствует

## Зависимости
- filesystem API
- `delete_safe` (для backup при overwrite)
- LSP или AST parser (для update_imports)

## Альтернативы
- Внешний filesystem MCP
- Ручное обновление импортов через `patch_file`

## Связанные функции
- `create_directory` — создать целевую директорию перед перемещением
- `delete_safe` — удаление старых файлов
- `get_references` — найти все импорты файла перед перемещением
