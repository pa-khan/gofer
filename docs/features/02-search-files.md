# 02. Search Files (Regex/Glob)

## Категория
Навигация и анализ (Поиск контекста)

## Приоритет
🔥 **P0** (Критично)

## Оценка полезности для AI
⭐⭐⭐⭐⭐ (5/5)

## Описание
Глобальный поиск по содержимому файлов с использованием regex-паттернов (аналог grep/ripgrep).

## Проблема
AI нужен быстрый текстовый поиск для нахождения определений функций, использования переменных, конфигурационных значений по всему проекту. Семантический поиск не всегда подходит для точного поиска строк.

## Сигнатура
```
search_files(directory, regex_pattern, file_extension, context_lines) -> SearchResults
```

## Параметры
- `directory` (string) — директория для поиска
- `regex_pattern` (string) — regex-паттерн для поиска
- `file_extension` (string, optional) — фильтр по расширению файла (например, `.rs`, `.ts`)
- `context_lines` (number, optional) — количество строк контекста до/после совпадения

## Возвращаемое значение
```json
{
  "matches": [
    {
      "file": "src/auth.rs",
      "line": 42,
      "column": 10,
      "match": "fn authenticate(token: &str)",
      "context_before": ["pub struct AuthService {", "    config: Config,"],
      "context_after": ["    // Verify JWT token", "    let claims = decode(token)?;"]
    }
  ],
  "total_matches": 15,
  "files_searched": 120
}
```

## Примеры использования

### Пример 1: Найти все использования функции
```
AI: search_files("src/", regex_pattern="authenticate\\(", file_extension=".rs")
Result: найдено 15 мест, где вызывается функция authenticate()
```

### Пример 2: Найти переменные окружения
```
AI: search_files(".", regex_pattern="process\\.env\\.", file_extension=".ts")
Result: найдены все места использования process.env.*
```

### Пример 3: Поиск с контекстом
```
AI: search_files("src/", regex_pattern="TODO:", context_lines=2)
Result: показывает TODO-комментарии с контекстом ±2 строки
```

## Преимущества
- **Дополнение к семантическому поиску**: точный текстовый поиск для literal strings
- **Быстрота**: regex-поиск быстрее семантического для простых запросов
- **Универсальность**: работает с любыми файлами, не только кодом

## Сложность реализации
Низкая (1 день) — можно использовать ripgrep как библиотеку

## Статус в gofer
✅ Частично: есть семантический `search`, но нет простого regex-поиска

## Зависимости
- ripgrep library (уже есть MCP интеграция в gofer)

## Альтернативы
- Использовать внешний ripgrep MCP-сервер
- Полагаться только на семантический поиск (менее точно)

## Связанные функции
- `search` (семантический поиск) — для концептуального поиска
- `get_references` — для поиска использований символов через LSP
