# 10. Create Directory

## Категория
Управление структурой (Management Tools)

## Приоритет
🟡 **P2** (Полезно)

## Оценка полезности для AI
⭐⭐⭐⭐ (4/5)

## Описание
Создание структуры папок с поддержкой вложенных директорий.

## Проблема
AI часто нужно создать новую структуру для модулей, компонентов, тестов. Без возможности создавать директории AI не может организовать код.

## Сигнатура
```
create_directory(path, recursive) -> DirectoryResult
```

## Параметры
- `path` (string) — путь к директории
- `recursive` (boolean, optional) — создать промежуточные директории (аналог `mkdir -p`), по умолчанию: true

## Возвращаемое значение
```json
{
  "path": "src/modules/auth/handlers",
  "status": "created",
  "created_parents": ["src/modules", "src/modules/auth", "src/modules/auth/handlers"],
  "already_existed": false
}
```

## Примеры использования

### Пример 1: Создание простой директории
```
AI: create_directory("src/utils")
Result: создана директория src/utils
```

### Пример 2: Создание вложенной структуры
```
AI: create_directory("src/modules/auth/handlers")
Result: созданы все промежуточные директории
```

### Пример 3: Директория уже существует
```
AI: create_directory("src/main")
Result: {status: "already_exists", path: "src/main"}
```

## Преимущества
- **Организация кода**: AI может создавать модульную структуру
- **Рефакторинг**: вынесение компонентов в отдельные папки
- **Безопасность**: не перезаписывает существующие директории

## Сложность реализации
Низкая (несколько часов)

## Статус в gofer
❌ Отсутствует

## Зависимости
- filesystem API

## Альтернативы
- Внешний filesystem MCP

## Связанные функции
- `move_file_or_directory` — перемещение в созданную директорию
- `write_file` с `create_dirs=true` — создаёт директории автоматически при записи файла
