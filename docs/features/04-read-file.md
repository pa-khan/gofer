# 04. Read File

## Категория
Умное чтение (Resources & Tools)

## Приоритет
🔥 **P0** (Критично)

## Оценка полезности для AI
⭐⭐⭐⭐⭐ (5/5)

## Описание
Стандартное чтение содержимого файла целиком.

## Проблема
Базовая необходимость для AI-агента. Без возможности читать файлы AI не может работать с кодом.

## Сигнатура
```
read_file(path) -> FileContent
```

## Параметры
- `path` (string) — путь к файлу

## Возвращаемое значение
```json
{
  "path": "src/main.rs",
  "content": "use std::collections::HashMap;\n\nfn main() {\n    ...\n}",
  "lines": 450,
  "size": "15.4 KB",
  "encoding": "utf-8"
}
```

## Примеры использования

### Пример 1: Чтение исходного кода
```
AI: read_file("src/auth.rs")
Result: возвращает полное содержимое файла
```

### Пример 2: Чтение конфигурации
```
AI: read_file(".env.example")
Result: читает переменные окружения
```

## Преимущества
- **Базовая функция**: без неё AI ничего не может делать
- **Простота**: самая простая операция чтения

## Недостатки
- **Расход токенов**: для больших файлов (>1000 строк) неэффективно
- **Лимиты**: файлы >100KB могут не поместиться в context window

## Сложность реализации
Очень низкая (несколько часов)

## Статус в gofer
❌ Отсутствует (предполагается использование внешнего MCP filesystem)

## Зависимости
- filesystem API

## Альтернативы
- `read_file_chunk` — для больших файлов
- `skeleton` — для получения только структуры

## Связанные функции
- `read_file_chunk` — чтение по частям
- `get_file_metadata` — проверка размера перед чтением
