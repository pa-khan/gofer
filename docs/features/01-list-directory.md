# 01. List Directory

## Категория
Навигация и анализ (Поиск контекста)

## Приоритет
🔥 **P0** (Критично)

## Оценка полезности для AI
⭐⭐⭐⭐⭐ (5/5)

## Описание
Возвращает структуру директории с поддержкой рекурсии и исключения тяжелых папок.

## Проблема
AI должен понимать, где он находится и как устроен проект, прежде чем что-то менять. Без возможности видеть структуру файлов AI "слеп" и не может ориентироваться в проекте.

## Сигнатура
```
list_directory(path, recursive, exclude_patterns) -> DirectoryTree
```

## Параметры
- `path` (string) — путь к директории
- `recursive` (boolean) — рекурсивный обход поддиректорий
- `exclude_patterns` (array<string>) — glob-паттерны для исключения (например, `node_modules`, `target`, `.git`)

## Возвращаемое значение
```json
{
  "path": "src/",
  "type": "directory",
  "children": [
    {
      "path": "src/main.rs",
      "type": "file",
      "size": 1024
    },
    {
      "path": "src/indexer/",
      "type": "directory",
      "children": [...]
    }
  ],
  "total_files": 42,
  "total_size": "1.2 MB"
}
```

## Примеры использования

### Пример 1: Просмотр структуры проекта
```
AI: list_directory("src/", recursive=true, exclude_patterns=["target", "node_modules"])
Result: показывает всю структуру src/ без тяжелых папок
```

### Пример 2: Быстрый просмотр одного уровня
```
AI: list_directory("src/indexer/", recursive=false)
Result: показывает только файлы в src/indexer/, без поддиректорий
```

## Преимущества
- **Экономия токенов**: исключение `node_modules`, `target` предотвращает переполнение контекста
- **Ориентация**: AI видит структуру проекта перед началом работы
- **Навигация**: AI может понять, где находятся нужные модули

## Сложность реализации
Низкая (1 день)

## Статус в gofer
❌ Отсутствует (есть только `get_symbols` для индексированных файлов)

## Зависимости
Нет

## Альтернативы
- Использовать внешний filesystem MCP-сервер
- Полагаться на `get_symbols` (но это не показывает структуру директорий)

## Связанные функции
- `get_file_metadata` — получить информацию о конкретном файле
- `search_files` — поиск файлов по паттерну
